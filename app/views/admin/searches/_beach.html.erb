<p class="beach">
  <% if f.object.new_record? %>
    <%= f.text_field :title %>
    <%= link_to_function "Удалить", "this.up('.beach').remove()" %>
  <% else %>
    <%= f.object.title %>
    <%= remove_link_unless_new_record(f) %>
  <% end %>
</p>

